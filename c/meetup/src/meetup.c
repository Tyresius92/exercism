#include "meetup.h"

int meetup_day_of_month(int year, int month, char *which, char *day)
{
        (void) year; 
        (void) month; 
        (void) which; 
        (void) day; 

        

        return 0; 
}